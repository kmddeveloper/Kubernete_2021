apiVersion: v1
kind: Service
metadata:
  name: {{ template "xunittest.fullname" . }}
  labels:
    app: {{ template "xunittest.name" . }}
    chart: {{ template "xunittest.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "xunittest.name" . }}
    release: {{ .Release.Name }}
